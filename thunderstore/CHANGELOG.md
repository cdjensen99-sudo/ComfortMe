# Changelog

## 0.1.0

- Hammer helper: live **This room: N**, color-coded **+N** chips on every comfort icon, and a group list vs this room.
- Green = would raise this room. Grey = equal or already in range (including unlit). Red = worse than the current group max.
- Unlit fires and a cold hot tub are treated as placed, not missing.
